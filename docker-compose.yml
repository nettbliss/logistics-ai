version: '3.8'

services:
  web:
    build: .
    ports:
      - '8000:8000'
    environment:
      - DEBUG=0
      - PYTHONUNBUFFERED=1
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
      - ./media:/app/media
    restart: unless-stopped
